import subprocess
subprocess.run(['ls'])   #list all the items in the current workoing directory
# subprocess.run(['ls','-la'])  #list all the (-la)items including hidden files in the current workoing directory